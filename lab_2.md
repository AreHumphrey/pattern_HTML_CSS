

# Лабораторная работа №2  
## **Исследование процессов, ресурсов и межпроцессного взаимодействия в Windows через терминал**



##  Задания

> **Каждое задание = 1 скриншот + 3–5 предложений пояснения.**  
> Сохраняйте скриншоты и вставьте в готовый отчет.

---

### **Задание 1. Анализ процессов: PID, память, потоки**  
Выполните в PowerShell:  
```powershell
Get-Process | Where-Object { $_.CPU -gt 0 } | Sort-Object CPU -Descending | Select-Object -First 5 -Property ProcessName, Id, CPU, WorkingSet, Threads
```  
Это покажет **5 самых нагруженных процессов** по CPU.

 **Скриншот:** результат команды.  
📝 **Дайте ответ**  
- Что означает `Id`?  
- Что такое `WorkingSet`?  
- Почему у одного процесса может быть много потоков (`Threads`)?  
- Как ОС использует PID для управления процессами?

---

### **Задание 2. Исследование дескрипторов (handles)**  
Дескрипторы — это ссылки процесса на ресурсы (файлы, окна, сокеты).  
Выполните (замените `notepad` на любой запущенный процесс, например `explorer`):  
```powershell
Get-Process notepad | Select-Object ProcessName, HandleCount
```  
Если Блокнот не запущен — откройте его.

**Скриншот:** вывод с `HandleCount`.  
📝 **Дайте ответ** 
- Что такое дескриптор?  
- Почему у процесса может быть сотни дескрипторов?  
- Может ли один процесс получить доступ к дескрипторам другого? Почему?

---

### **Задание 3. Обнаружение именованных каналов (Named Pipes) — IPC в действии**  
Именованные каналы — один из ключевых механизмов **межпроцессного взаимодействия (IPC)** в Windows.

Выполните:  
```powershell
Get-ChildItem \\.\pipe\ | Measure-Object
```  
→ Увидите **общее количество** каналов.  
Затем:  
```powershell
Get-ChildItem \\.\pipe\ | Select-Object -First 10 -ExpandProperty Name
```  

 **Скриншот:** список из 10 имён каналов.  
📝**Дайте ответ**   
- Что такое именованный канал?  
- Какие программы, по вашему мнению, создали эти каналы (например, `chrome`, `docker`)?  
- Почему каналы — эффективный способ IPC?

---

### **Задание 4. Моделирование гонки данных через файл**  
Создадим ситуацию, при которой **два процесса одновременно пишут в один файл** — это демонстрирует **проблему синхронизации в IPC**.

1. В **двух разных окнах PowerShell** выполните (одновременно!):  
   ```powershell
   1..50 | ForEach-Object { Add-Content -Path "$env:USERPROFILE\Desktop\Lab2_OS\race.txt" -Value "Поток A: $_" }
   ```  
   и  
   ```powershell
   1..50 | ForEach-Object { Add-Content -Path "$env:USERPROFILE\Desktop\Lab2_OS\race.txt" -Value "Поток B: $_" }
   ```  
2. Откройте файл `race.txt` в Блокноте.

**Скриншот:** фрагмент файла с **перемешанными/обрывочными строками**.  
📝 **Дайте ответ** 
- Почему строки перемешаны?  
- Что такое **гонка данных (race condition)**?  
- Какие IPC-механизмы (например, **мьютексы**) могли бы предотвратить эту проблему?

---

### **Задание 5. Анализ времени выполнения: режим ядра vs пользовательский**  
Процессы тратят время в **пользовательском режиме** (свой код) и **режиме ядра** (системные вызовы).

Выполните:  
```powershell
Get-WmiObject Win32_Process | Where-Object { $_.Name -like "*notepad*" } | Select-Object ProcessName, KernelModeTime, UserModeTime
```  
(Если Блокнот не запущен — откройте его.)

 **Скриншот:** значения `KernelModeTime` и `UserModeTime`.  
📝 **Дайте ответ** 
- В чём разница между режимами?  
- Почему у простого Блокнота почти нет времени в режиме ядра?  
- Как это связано с **безопасностью** и **изоляцией**?

---

### **Задание 6**  


Ответьте  :  
1. Какие **два механизма IPC в Windows** можно использовать?  
2. Сравните их по критериям: **скорость**, **надёжность**, **безопасность**.  
3. Какой вы выберете и почему?



---


### **Задание 7. Анализ родительско-дочерних связей процессов**  
 **Родительский процесс** — процесс, который **породил** другой процесс (дочерний) с помощью системного вызова (например, `CreateProcess` в Windows).  
 **Дочерний процесс** — новый процесс, созданный родителем. Наследует часть окружения, но работает независимо.  
**ParentProcessId (PPID)** — PID родительского процесса, сохраняется в структуре процесса.

📋 Выполните:  
```powershell
Start-Process notepad
Get-CimInstance Win32_Process | Where-Object { $_.Name -like "*notepad*" } | Select-Object ProcessId, ParentProcessId, Name
```

Скриншот: PID и PPID Блокнота.  
📝**Дайте ответ**  
- Что такое родительский процесс?  
- Почему ОС отслеживает эту связь?  
- Может ли дочерний процесс “пережить” родителя?

---

### **Задание 8. Исследование объектов синхронизации через имена**  
 **BaseNamedObjects** — раздел **объектного менеджера ядра Windows**, в котором хранятся **именованные объекты синхронизации**, доступные всем процессам текущего пользователя:  
- **Мьютексы (Mutex)** — для взаимного исключения,  
- **События (Events)** — для сигнализации,  
- **Семафоры** — для ограничения числа одновременных пользователей ресурса,  
- **Разделяемая память (Shared Memory)** — участки памяти с именем.

📋 Выполните:  
```powershell
Get-ChildItem \\.\BaseNamedObjects\ | Select-Object -First 10 -ExpandProperty Name
```

Скриншот: список имён (или сообщение об ошибке).  
📝**Дайте ответ**  
- Что такое `BaseNamedObjects`?  
- Какие объекты там хранятся?  
- Почему доступ ограничен? Как это связано с безопасностью IPC?

---

### **Задание 9. Сравнение ресурсов: GUI-приложение vs консольное**  
 **GUI-приложение (Graphical User Interface)** — программа с окнами, кнопками, мышью. Требует взаимодействия с **графической подсистемой Windows (Desktop Window Manager)**.  
 **Консольное приложение** — работает в терминале, использует текстовый ввод/вывод, минимально взаимодействует с GUI.

📋 Выполните:  
```powershell
Get-Process | Where-Object { $_.ProcessName -in @("notepad", "powershell") } | Select-Object ProcessName, Handles, Threads, WorkingSet
```

 Скриншот: сравнение ресурсов.  
📝**Дайте ответ**   
- Почему у GUI-приложения больше дескрипторов и потоков?  
- Как графическая подсистема влияет на ресурсы?  
- Какие системные вызовы чаще использует GUI?

---

### **Задание 10. Моделирование deadlock через файловую блокировку**  
 **Блокировка файла (File Lock)** — механизм, при котором процесс, открывший файл, **запрещает другим процессам** изменять или удалять его до закрытия.  
 **Взаимная блокировка (Deadlock)** — ситуация, при которой два или более процессов **взаимно ожидают** освобождения ресурсов друг друга и **никто не может продолжить работу**.

📋  
1. Откройте `test.txt` в Блокноте (не закрывайте!).  
2. В PowerShell:  
```powershell
Remove-Item "$env:USERPROFILE\Desktop\Lab2_OS\test.txt" -ErrorAction SilentlyContinue
```

Скриншот: ошибка удаления.  
📝**Дайте ответ**  
- Почему файл нельзя удалить?  
- Как ОС реализует блокировку?  
- Как это связано с deadlock?

---

### **Задание 11. Анализ сетевых сокетов как IPC-механизма**  
 **Сокет (Socket)** — конечная точка сетевого соединения. В Windows сокеты используются не только для интернета, но и для **локального IPC через loopback-интерфейс (`127.0.0.1`)**.  
 **localhost (127.0.0.1)** — виртуальный сетевой интерфейс, позволяющий программам на одном компьютере обмениваться данными **как по сети**.

📋 Выполните:  
```powershell
Get-NetTCPConnection | Where-Object { $_.State -eq "Listen" -and $_.LocalAddress -eq "127.0.0.1" } | Select-Object LocalPort, OwningProcess -First 5
```
Затем для одного PID:  
```powershell
Get-Process -Id <PID>
```

Скриншот: список сокетов и имя процесса.  
📝**Дайте ответ**  
- Почему приложения используют `127.0.0.1` для IPC?  
- В чём преимущество сокетов?  
- Как это связано с микросервисами?

---

### **Задание 12. Исследование изоляции: может ли процесс читать память другого?**  
 **Адресное пространство** — диапазон виртуальных адресов, выделенный процессу. Другие процессы **не могут** читать или писать в него напрямую — это обеспечивает **защиту и стабильность системы**.  
 **Изоляция процессов** — фундаментальный принцип ОС: сбой в одном приложении не влияет на другие.

1. В Блокноте №1 напишите: `Секретный ключ: 7X9L2`.  
2. В Блокноте №2 попытайтесь найти этот текст через поиск, PowerShell и т.д.

Скриншот: результат поиска (скорее всего — не найдено).  
📝**Дайте ответ**  
- Почему вы не смогли прочитать содержимое?  
- Как ОС обеспечивает изоляцию?  
- Какие исключения существуют (и почему они требуют админских прав)?


## 📤 Требования к отчёту  
- Формат: **PDF или Word** .  
- Структура:  
  - Нумерация
  - Ответы на все вопросы
